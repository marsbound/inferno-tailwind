const CenteredHero = ({ title }) => (
  <div class="animated fadeIn w-full h-screen flex justify-center items-center">
    <p class="tracking-widest text-2xl font-light uppercase">{title}</p>
  </div>
);

export default CenteredHero;
